import React from 'react';

interface IProps {
    id: Number;
}

const TicketMain = (props: IProps) => {
    console.log()
    return (
        <div>Ticket main</div>
    )
}

export default TicketMain